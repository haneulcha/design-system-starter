---
version: alpha
name: FixtureYaml
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    rounded: "{rounded.md}"
    padding: 12px 20px
    height: 40px
  button-secondary:
    backgroundColor: "{colors.canvas}"
    rounded: "{rounded.md}"
  search-orb:
    backgroundColor: "{colors.primary}"
    rounded: "{rounded.full}"
  top-nav:
    backgroundColor: "{colors.canvas}"
    height: 64px
  badge-pill:
    rounded: "{rounded.pill}"
---

## Components

prose follows.
